export interface RouterData {
    origin: {
        latitude: number; 
        longitude: number; 
      },
      destination: {
        latitude: number; 
        longitude: number;
      },
      distance: number,
      duration: string;
  }

